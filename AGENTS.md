# AGENTS.md

このリポジトリで作業する前に、ルートの `CLAUDE.md` を最初から最後まで読むこと。

変更対象が `server/`、`web/`、`widget/` 配下の場合は、各ディレクトリの `CLAUDE.md` も最初から最後まで読み、ルートとあわせて従うこと。

プロジェクトの説明、構成、コーディング規約、品質ゲートは `CLAUDE.md` を正とし、このファイルには重複して記載しない。
